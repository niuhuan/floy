
import 'package:flutter/material.dart';

class ContentBlank extends StatelessWidget {
  const ContentBlank({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      // todo 一个空盒子的图片
      child: Text("这里什么都没有"),
    );
  }
}
